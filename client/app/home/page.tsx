import styles from '../../styles/home.module.scss';
import Landing from '../../components/Landing';

const Home = () => {
  return (
    <div>
      <Landing />
    </div>
  );
};

export default Home;
